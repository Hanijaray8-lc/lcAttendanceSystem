services:
  - type: web
    name: lms-server
    runtime: node
    rootDir: server
    buildCommand: npm install && cd ../client && npm install --include=dev && npm run build
    startCommand: node index.js
    envVars:
      - key: NODE_ENV
        value: production
